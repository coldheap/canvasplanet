# Repository working agreements

- Every agent working in this repository must use Git.
- Inspect `git status` before editing and review the final diff before handoff.
- Save completed changes in a focused Git commit unless the user explicitly requests no commit.
- Preserve unrelated worktree changes and keep them out of the task's commit.
- Do not add subtitles or helper copy unless they provide context necessary to understand or use the interface.
